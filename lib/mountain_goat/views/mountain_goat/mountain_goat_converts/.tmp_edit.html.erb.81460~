<%= render :partial => 'ocelot/ocelot_users/user_bar' %>

<div id="container-main" class="mt-converts">
	<div class="ocelot-panel centered">
		<h2>Editing conversion goal</h2>
		
		<% form_for :convert, @convert, :url => ocelot_ocelot_convert_url(:id => @convert.id), :html => { :method => :put } do |f| %>
			<%= f.error_messages %>
		
			<%= render :partial => 'ocelot/ocelot_converts/convert_form', :locals => { :f => f } %>
		<% end %>
	</div>
</div>