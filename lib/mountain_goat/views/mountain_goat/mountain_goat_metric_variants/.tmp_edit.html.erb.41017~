<%= render :partial => 'ocelot/ocelot_users/user_bar' %>

<div id="container-main" class="mt-metric-variants">
	<div class="ocelot-panel centered">
		<h2>Edit metric variant</h2>
		
		<% form_for :metric_variant, @metric_variant, :url => ocelot_ocelot_metric_variant_url(:id => @metric_variant.id), :html => { :method => :put } do |f| %>
			<%= f.error_messages %>
		
			<%= render :partial => 'ocelot/ocelot_metric_variants/metric_variant_form', :locals => { :f => f } %>
		<% end %>
	</div>
</div>