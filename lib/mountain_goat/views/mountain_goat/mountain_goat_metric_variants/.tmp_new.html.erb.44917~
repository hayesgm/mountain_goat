<%= render :partial => 'ocelot/ocelot_users/user_bar' %>

<div id="container-main" class="mt-metric-variants">
	<div class="ocelot-panel cenetered">
		<h2>New metric variant for</br> <%=h @metric.title %></h2>
		
		<% form_for :metric_variant, @metric_variant, :url => ocelot_ocelot_metric_variants_url do |f| %>
			<%= f.error_messages %>
		
			<%= render :partial => 'ocelot/ocelot_metric_variants/metric_variant_form', :locals => { :f => f } %>
		<% end %>
		
		<a href="<%= ocelot_ocelot_metric_url :id => @metric.id %>">Back to <%=h @metric.title %></a>
	</div>
</div>