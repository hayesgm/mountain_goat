<%= render :partial => 'ocelot/ocelot_users/user_bar' %>

<div id="container-main" class="mt-metric-variants">
	<div class="ocelot-panel cenetered">
		<a href="<%= ocelot_ocelot_converts_url %>">Goals</a> > <a href="<%= ocelot_ocelot_convert_url :id => @metric_variant.metric.convert.id %>"><%=h @metric_variant.metric.convert.name %> Goal</a> > <a href="<%= ocelot_ocelot_metric_url :id => @metric_variant.metric.id %>"><%=h @metric_variant.metric.title %></a> 
		
		<h2><%=h @metric_variant.name %></h2>
		
		<div class="value"><%=h @metric_variant.value %></div>
		<div class="serves-converts"><%=h @metric_variant.served %> / <%=h @metric_variant.conversions %> (<%=h number_to_percentage(@metric_variant.conversion_rate, :precision => 0) %>)</div>
	
		<a href="<%= edit_ocelot_ocelot_metric_variant_url :id => @metric_variant.id %>">Edit</a>
	</div>
</div>